{
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Odin Recipes",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}